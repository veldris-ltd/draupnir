/**
 * JARNGREIPR, the DRAUPNIR design system.
 *
 * The token values, the component library and the six universal states are
 * specified in VLD-UX-DRAUPNIR-001 and built in Prompts UX-1 to UX-4. This
 * package exists now with its build, its Storybook and its accessibility and
 * visual regression gates already wired, so that the first component arrives
 * into a working harness rather than dragging one behind it.
 */

/** The version of the design system, surfaced in the console footer. */
export const JARNGREIPR_VERSION = '0.1.0';
