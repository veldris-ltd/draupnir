# AC-F18

> A second site is registered and a run is submitted to it, with site scope resolving correctly in every artefact URI and ledger entry

**Priority:** Must
**Status:** IMPLEMENTED

## How it is demonstrated

A second site is seeded and every read is scoped; row level security returns zero rows to a session that has not chosen one.


```bash
make test-integration
```

## Evidence

- `draupnir/api/reading.py`
- `draupnir/api/routers/sites.py`
- `draupnir/hodd/stores.py`
- `skills/draupnir-endpoint/scripts/new_endpoint.py`
- `web/packages/api-client/src/generated/schema.d.ts`

---

Generated by `scripts/acceptance.py` from SAD 12 and from the citations in the
repository. Edit the register in that script, not this file.
