# AC-N7

> The application runs on aarch64 Linux and on Apple silicon macOS from the same source

**Priority:** Target
**Status:** DEVIATED

## How it is demonstrated

The core has no conditional code path on platform; every driver renders the same plan on either architecture.

Exercised on win32 and on the linux/arm64 image build. It has not run on Apple silicon macOS, which is one of the two deployment targets. The Sindri estate does not exist yet: SAD 1.3 puts the hardware build in VLD-INF-SINDRI-001 and out of scope here. Everything the control plane owns is implemented and exercised; the measurement this criterion asks for is taken at commissioning, on the estate, and is not one a developer machine can stand in for.


```bash
make test-contract
```

## Evidence

- `draupnir/hodd/manifest.py`
- `plugins/targz_export/draupnir_targz_export/__init__.py`

---

Generated by `scripts/acceptance.py` from SAD 12 and from the citations in the
repository. Edit the register in that script, not this file.
