# AC-S18

> No release metadata reaches any external transparency log. Verified by network capture during a release

**Priority:** Must
**Status:** DEVIATED

## How it is demonstrated

The transparency log is self-hosted at MEGINGJORD and nothing reaches an external one; the egress allow list has no entry that could.

Verified by inspection of the allow list and the release path rather than by network capture: there is no deployed release to capture. The Sindri estate does not exist yet: SAD 1.3 puts the hardware build in VLD-INF-SINDRI-001 and out of scope here. Everything the control plane owns is implemented and exercised; the measurement this criterion asks for is taken at commissioning, on the estate, and is not one a developer machine can stand in for.


```bash
make test-unit
```

## Evidence

- `draupnir/svalinn/pki.py`
- `tests/unit/test_svalinn_security.py`

---

Generated by `scripts/acceptance.py` from SAD 12 and from the citations in the
repository. Edit the register in that script, not this file.
