# AC-U16

> First contentful paint under 1.5 s, interaction to next paint under 200 ms at the 75th percentile, initial bundle under 300 KB gzipped

**Priority:** Should
**Status:** DEVIATED

## How it is demonstrated

The console bundle is 66.66 kB gzipped against a 300 kB budget, checked in the build.

First contentful paint and interaction to next paint are field percentiles. There is no deployment and no field, so the bundle budget is measured and the two timing percentiles are not.


```bash
make build-web
```

## Evidence

- `web/apps/console/src/routing.ts`

---

Generated by `scripts/acceptance.py` from SAD 12 and from the citations in the
repository. Edit the register in that script, not this file.
