# AC-F5

> A fifty six element adapter array is submitted as one action and executes exactly three concurrently, one per appliance

**Priority:** Must
**Status:** DEVIATED

## How it is demonstrated

A fifty six element array is built as one plan and capped at one element per available appliance.

The Sindri estate does not exist yet: SAD 1.3 puts the hardware build in VLD-INF-SINDRI-001 and out of scope here. Everything the control plane owns is implemented and exercised; the measurement this criterion asks for is taken at commissioning, on the estate, and is not one a developer machine can stand in for. No array has executed on three appliances.


```bash
make test-unit
```

## Evidence

- `draupnir/motsognir/arrays.py`
- `plugins/motsognir_slurm/draupnir_motsognir_slurm/__init__.py`
- `tests/unit/test_placement_and_arrays.py`

---

Generated by `scripts/acceptance.py` from SAD 12 and from the citations in the
repository. Edit the register in that script, not this file.
