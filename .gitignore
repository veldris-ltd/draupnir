# Python
__pycache__/
*.py[cod]
.venv/
.uv-bootstrap/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.hypothesis/
.coverage
.coverage.*
coverage.xml
coverage-contract.xml
coverage-integration.xml
htmlcov/
/dist/
/build/
*.egg-info/

# Node
node_modules/
web/**/dist/
web/**/dist-types/
web/**/*.tsbuildinfo
web/**/storybook-static/
web/**/.vite/
web/**/playwright-report/
web/**/test-results/
web/**/blob-report/
.pnpm-store/

# Local state
.env
.env.local
docker/.env
artefacts/
*.log

# Generated evidence, rebuilt by the pipeline
sbom/

# Run records: the keyboard walk and Procedures M1 to M10 write here, each with
# its own clock and database state, so a committed copy was rewritten by every
# run (RF-31). The pipeline writes them on every build and uploads them.
docs/acceptance/evidence/

# Playwright output. Recorded visual baselines are committed; failures are not.
web/**/*-actual.png
web/**/*-diff.png
