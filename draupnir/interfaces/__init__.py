"""Ports: the seven Protocols and the conformance harness.

Knows interfaces, never implementations (SAD 11B). The Protocols themselves
are defined in Prompt 2; this package exists now so that the import contract
in `.importlinter` is enforced from the first module rather than added later.
"""
